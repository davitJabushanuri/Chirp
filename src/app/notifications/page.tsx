import { NotificationsHeader } from "@/features/header";

const Notifications = () => {
  return (
    <div>
      <NotificationsHeader />
    </div>
  );
};

export default Notifications;

export const metadata = {
  title: "Notifications",
};
